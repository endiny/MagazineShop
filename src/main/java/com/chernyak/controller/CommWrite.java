package com.chernyak.controller;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class CommWrite extends Command {
	
    /**
     * Note to study the selected student
     * @param request
     * @param response
     * @throws ServletException
     * @throws IOException
     */
    @Override
	public void execute(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		Integer grade = Integer.valueOf(request.getParameter("grade"));
		String comment = request.getParameter("comment");
		Integer id = Integer.valueOf(request.getParameter("id"));
		Boolean result = clientService.gradeStudent(id, grade, comment);
		String massage = null ;
		if (result) {
			massage =  "???? ?????????????? ???????????????? ????????????/?????????????????????? ?????? ????????????????.";
		} else {
			massage = "?????? ???? ?????????? ???? ??????. ???????????????????? ?????????????????? ????????????????????.";
		}
		request.setAttribute("massege", massage);
		request.getRequestDispatcher("/WEB-INF/end.jsp").forward(request, response);
		
	}

}
